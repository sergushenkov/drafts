. как и в LinkedList не считал ошибкой пустой список при использовании методов head(), tail(), put_right(), put_left(), remove(), replace() - поэтому у меня нет запросов этих статусов
. Не подумав, прописал метод get_left_status в родительском классе, т.е. получается он будед доступен и для LinkedList. Проблем не вызовет, но там это лишнее. С другой стороны - если он будет только в классе TwoWayList, то для этого класса придётся также переопределять методы __init__() и clear(). Пока не знаю как лучше
. В остальном - совпадает с эталонным решением