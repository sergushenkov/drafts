. Разница в именовании функции в родительском классе - вроде ерунда, но когда заметил, понимаю что не дело. Везде использую front, а в одной конкретной функции get_head() - путаницу создаёт
. Второе отличие - я не разделял статусы _get_front_status и _get_tail_status, а вместо этого использовал _get_status() - для обоих функций чтения. Аналогично - _remove_status. С учётом того что оба варианта _get_status возвращают либо ОК, либо Очередь пуста - мне кажется это приемлемо
. В идеальном варианте для Deque прописана своя функция size() - у меня Deque использует родительскую функцию без переопределения. Думаю, в данном случае, в идеальном варианте такая же функция
